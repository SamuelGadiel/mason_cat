import '../../domain/entities/{{module_name}}.dart';

class {{module_name.pascalCase()}}Mapper {
  static {{module_name.pascalCase()}} fromJson(Map<String, dynamic> json){
    return {{module_name.pascalCase()}}(

    );
  }
}
