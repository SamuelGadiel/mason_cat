import '../../domain/entities/{{> parameters.snakeCase() }}.dart';

class {{> parameters }}Mapper {
  static Map<String, dynamic> toJson({{> parameters }} parameters) {
    return {
      
    };
  }
}
